import React from "react";

const Homepage = () => {
  return (
    <div>
      <p>
        Welcome to the default Homepage - select an option to view students by
        block.
      </p>
    </div>
  );
};

export default Homepage;
